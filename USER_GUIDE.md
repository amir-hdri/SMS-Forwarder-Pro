# BarPro SMS Forwarder & RPA Waybill Automation - راهنمای جامع کاربری و استقرار (User & Operations Guide)

این راهنما شامل دستورالعمل‌های گام‌به‌گام برای رانندگان ناوگان، مدیران شرکت‌های حمل‌ونقل و تیم‌های دواپس (DevOps) جهت راه‌اندازی و استفاده بهینه از کلاینت اندروید و سرور اتوماسیون بارنامه است.

---

## بخش اول: راهنمای رانندگان ناوگان و کاربران اندروید

### ۱. نصب و راه‌اندازی اولیه
1. دستگاه با سیستم‌عامل **اندروید ۸.۰ (API 26) یا بالاتر**.
2. فایل نصبی برنامه (`app-debug.apk` یا فایل نصبی منتشرشده در پنل رانندگان) را دانلود و روی گوشی تلفن همراه راننده نصب کنید.
3. اپلیکیشن را اجرا نمایید. در اولین اجرا، صفحه **مدیریت مجوزها (Permission Manager)** نمایش داده می‌شود.

### ۲. اعطای دسترسی‌های چهارگانه (بسیار مهم)
برای تضمین دریافت ۱۰۰٪ پیامک‌ها و ارسال بلادرنگ حتی در شرایط قطعی کامل اینترنت جاده‌ای:
- **دسترسی‌های پیامک (SMS Permissions)**: روی دکمه «اعطای دسترسی پیامک» کلیک کرده و گزینه *Allow* را انتخاب فرمایید تا رسیور بتواند پیامک‌های تایید بارنامه را پردازش و در صورت قطعی اینترنت فالبک اضطراری ارسال کند (`RECEIVE_SMS`, `READ_SMS`, `SEND_SMS`).
- **دسترسی اعلان‌ها (Notification Permission)**: برای نمایش اعلان مداوم سرویس فورگراند در نوار اعلان و جلوگیری از متوقف شدن پردازش، این دسترسی را فعال نمایید (`POST_NOTIFICATIONS`).
- **عدم بهینه‌سازی باتری (Ignore Battery Optimization)**: به سیستم‌عامل اجازه دهید فورواردر را از لیست مصرف باتری بهینه‌شده مستثنی کند تا سیستم مدیریت حافظه اندروید در سفرهای طولانی جاده‌ای برنامه را نبندد.
- **شنونده نوتیفیکیشن‌ها (Notification Listener - اختیاری اما توصیه‌شده)**: در گوشی‌های سامسونگ و شیائومی (اندروید ۱۱ تا ۱۴)، دسترسی «شنونده اعلان» را فعال کنید تا در صورت تاخیر سیستم در ارسال برودکست پیامک، برنامه پیامک را از نوتیفیکیشن پیام‌رسان استخراج نماید.

### ۳. تنظیمات وب‌هوک و اتصال به سامانه صدور بارنامه
1. از منوی پایین صفحه، وارد بخش **تنظیمات سرور (Settings)** شوید.
2. برای استفاده از درگاه شبانه بارپرو، کافیست روی دکمه **«تنظیم خودکار وب‌هوک RPA بارپرو (صندوق OTP)»** ضربه بزنید. تنظیمات زیر به صورت خودکار اعمال خواهند شد:
   - **آدرس وب‌هوک**: `https://api.barpro.ir/api/v1/rpa/sms-forwarder`
   - **نوع اتصال**: هدر اختصاصی `X-Forwarder-Secret`
   - **حالت امنیتی**: اعتبارسنجی ارقام و کد ۵ رقمی سامانه شهرداری
3. کلید محرمانه (`Secret Key`) ارائه شده توسط شرکت بارپرو را در فیلد مربوطه وارد کنید.
4. **شماره سیم‌کارت سرور (پیامک اضطراری / مودم GSM)**: شماره خط اختصاصی مودم سرور بارپرو (مثلا `09120000000` یا شماره سرشماره شرکت) را وارد نمایید.
   - *مزیت حیاتی*: در زمان‌هایی که اینترنت جاده‌ای قطع است یا با تاخیر مواجه می‌شود، برنامه بدون اتلاف وقت، پیامک رمزارز را در قالب کد فشرده امن به این شماره پیامک می‌کند و ربات بارنامه را صادر می‌نماید!
   - *پشتیبانی از دو سیم‌کارت*: اپلیکیشن به صورت هوشمند اسلات سیم‌کارتی که پیامک را دریافت کرده انتخاب می‌کند تا پیامک فالبک نیز از همان خط ارسال شود.
5. دکمه **«بررسی اتصال سرور»** را بزنید تا از برقراری ارتباط اطمینان حاصل شود.

### ۴. فعال‌سازی و آزمایش سیستم
- در صفحه اصلی (داشبورد)، کلید بزرگ وضعیت را روی حالت **«سرویس فعال»** قرار دهید.
- جهت تست کارکرد سیستم بدون نیاز به ارسال پیامک واقعی:
  1. روی دکمه **«شبیه‌ساز پیامک (Test SMS)»** کلیک کنید.
  2. متن پیش‌فرض کد ورود سامانه شهرداری را انتخاب نمایید.
  3. دکمه «ارسال آزمایشی» را بفشارید و در بخش **لاگ‌ها (Logs)** ثبت موفقیت‌آمیز رویداد را بررسی کنید.
- همچنین با لمس دکمه **«استعلام کدهای بارنامه»**، می‌توانید آخرین کدهای دریافتی و کد رهگیری‌ها را مستقیماً کپی کنید.

---

## بخش دوم: راهنمای مدیران سیستم و تیم‌های زیرساخت (DevOps)

### ۱. معماری و پیش‌نیازهای سرور
- **پایتون**: نسخه 3.11 یا جدیدتر
- **ردیس (Redis)**: نسخه 7.0 یا جدیدتر با پشتیبانی از Pub/Sub
- **پلی‌رایت (Playwright)**: نصب مرورگر کرومیوم جهت وب اسکرپینگ و تزریق خودکار فرم

### ۲. متغیرهای محیطی کلیدی (.env)
```env
# کلید اختصاصی احراز هویت وب‌هوک رانندگان
SMS_FORWARDER_SECRET=your-super-secure-production-secret-token

# آدرس اتصال به ردیس مقتدرانه
REDIS_URL=redis://localhost:6379/0

# محیط اجرا (production / staging / development)
ENVIRONMENT=production

# پورت و هاست اجرای سرور
PORT=8000
HOST=0.0.0.0
```

### ۳. استقرار بک‌اند با Uvicorn و Systemd
یک سرویس در مسیر `/etc/systemd/system/barpro-rpa.service` ایجاد نمایید:
```ini
[Unit]
Description=BarPro RPA SMS Forwarder & Waybill Automation
After=network.target redis.service

[Service]
User=barpro
WorkingDirectory=/opt/barpro/backend
EnvironmentFile=/opt/barpro/backend/.env
ExecStart=/opt/barpro/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

فعال‌سازی سرویس:
```bash
sudo systemctl daemon-reload
sudo systemctl enable --now barpro-rpa
```

### ۴. آزمایش اندپوینت‌ها با cURL

#### الف) شبیه‌سازی ارسال سریع اینترنتی (Fast-Path HTTP):
```bash
curl -X POST https://api.barpro.ir/api/v1/rpa/sms-forwarder \
  -H "Content-Type: application/json" \
  -H "X-Forwarder-Secret: your-super-secure-production-secret-token" \
  -d '{
    "phone": "09333702137",
    "text": "کد ورود شما به سامانه بارنامه برخط شهرداری: ۹۲۸۱۵",
    "sender": "10008545",
    "timestamp": 1725538341000
  }'
```

#### ب) شبیه‌سازی دریافت پیامک اضطراری از مودم سیم‌کارتی سرور (GSM Inbound Relay):
```bash
curl -X POST https://api.barpro.ir/api/v1/rpa/sms-gateway/webhook \
  -H "Content-Type: application/json" \
  -H "X-Forwarder-Secret: your-super-secure-production-secret-token" \
  -d '{
    "from": "09333702137",
    "text": "BARPRO#DRV-908172#09333702137#UTCMS_OTP#92815"
  }'
```

**پاسخ مورد انتظار (200 OK):**
```json
{
  "success": true,
  "status": "success",
  "phone": "0933***2137",
  "message": "Gateway OTP accepted and stored in vault",
  "otp_detected": true,
  "is_duplicate": false
}
```

### ۵. اتصال مودم فیزیکی GSM به سرور لینوکس (اختیاری اما توصیه تجاری)
برای تضمین دریافت کدهای رانندگان در جاده‌های فاقد اینترنت، کافیست یک دانگل سیم‌کارتی (مثلا Huawei E3372 یا D-Link) به پورت USB سرور متصل کرده و از دیمن `smsd` (نرم‌افزار SMS Server Tools 3) یا `gammu-smsd` استفاده کنید. در فایل کانفیگ دیمن، اسکریپت رویداد دریافتی (`eventhandler`) را به صورت زیر تنظیم کنید تا به محض دریافت پیامک، وب‌هوک فراخوانی شود:

```bash
#!/bin/bash
# /opt/barpro/scripts/on_sms_received.sh
FROM="$1"
TEXT="$2"
curl -s -X POST http://127.0.0.1:8000/api/v1/rpa/sms-gateway/webhook \
  -H "Content-Type: application/json" \
  -H "X-Forwarder-Secret: $SMS_FORWARDER_SECRET" \
  -d "{\"from\": \"$FROM\", \"text\": \"$TEXT\"}"
```

---

## بخش سوم: راهنمای رفع اشکال متداول (Troubleshooting)

| مشکل مشاهده‌شده | علت احتمالی | راه‌حل |
|---|---|---|
| اینترنت راننده در جاده قطع است ولی بارنامه باید صادر شود | قطع آنتن دیتا / اتمام بسته | **حل‌شده با فالبک اضطراری**: شماره سیم‌کارت سرور را در صفحه تنظیمات اپ وارد کنید و مجوز ارسال پیامک (`SEND_SMS`) را بدهید. برنامه در ۰ تا ۱.۵ ثانیه پیامک اضطراری را به سرور رله کرده و بارنامه در ۳ تا ۵ ثانیه صادر می‌شود. |
| خطای `401 Unauthorized` در وب‌هوک اینترنتی یا مودم GSM | عدم تطابق هدر `X-Forwarder-Secret` با تنظیمات سرور | کلید امنیتی تنظیم‌شده در تب تنظیمات اپلیکیشن را با فایل محیطی سرور تطبیق دهید. |
| پیامک دریافت شد اما `otp_detected: false` است | پیامک ارسالی حاوی کد ۵ رقمی استاندارد نیست | مطمئن شوید پیامک مربوط به سامانه‌های مجاز (بارنامه UTCMS، سهمیه سوخت، کارت سوخت، باربرگ) بوده و کد ۵ رقمی در آن موجود است. |
| پیامک فالبک از سیم‌کارت اشتباهی راننده ارسال می‌شود | وجود دو سیم‌کارت فعال در گوشی | سیستم به صورت هوشمند اسلات سیم‌کارت دریافتی را تشخیص می‌دهد؛ در صورت نیاز در تنظیمات پیشرفته می‌توانید اسلات پیش‌فرض را بررسی فرمایید. |
| متوقف شدن سرویس پس از چند ساعت خاموشی صفحه | غیرفعال بودن معافیت باتری در دستگاه | از منوی Permission Manager برنامه، دسترسی *Ignore Battery Optimization* را مجدداً فعال فرمایید. |
| پیامک تکراری در سیستم ثبت می‌شود | ارسال همزمان از طریق رسیور و نوتیفیکیشن لیسنر | نگران نباشید؛ سیستم بک‌اند با تولید کلید انگشت‌نگاری SHA-256 درخواست دوم را به صورت امن و Idempotent شناسایی کرده و پاسخ موفق تاییدیه بازمی‌گرداند. |


