# BarPro SMS Forwarder & RPA Waybill Automation - معماری فنی سیستم (Technical Architecture)

این سند معماری فنی، لایه‌بندی نرم‌افزاری، الگوهای طراحی و پروتکل‌های امنیتی کل سامانه بارپرو را تشریح می‌کند. سامانه از دو بخش اصلی تشکیل شده است:
۱. **کلاینت اندروید (BarPro Android Forwarder)**: دریافت بی‌وقفه پیامک‌های سامانه‌های حمل‌ونقل و راننده در هر شرایط آب‌وهوایی و جاده‌ای.
۲. **بک‌اند پردازش و اتوماسیون (FastAPI + Redis Vault + Playwright RPA)**: وب‌هوک پرسرعت، صندوق امن رمز یکبار مصرف (OTP) و ربات هوشمند صدور بارنامه در سامانه UTCMS شهرداری.

---

## ۱. دیاگرام کلان جریان داده سرتاسری (End-to-End System Flow)

```
[درگاه پیامک UTCMS / مخابرات]
           │
           │ پیامک کد تایید / بارنامه
           ▼
┌────────────────────────────────────────────────────────────────────────┐
│                   دستگاه موبایل راننده / ناوگان (Android Client)        │
│                                                                        │
│   ┌───────────────────────────┐      ┌─────────────────────────────┐   │
│   │    SmsReceiver (Primary)  │      │ SmsNotificationListener     │   │
│   │    Telephony.SMS_RECEIVED │      │ (Fallback on Android 11-14) │   │
│   └─────────────┬─────────────┘      └──────────────┬──────────────┘   │
│                 │                                   │                  │
│                 └─────────────────┬─────────────────┘                  │
│                                   ▼                                    │
│       ┌───────────────────────────────────────────────────────┐        │
│       │ SmsParser & Normalizer                                │        │
│       │ - تبدیل ارقام فارسی و عربی (۰-۹ -> 0-9)              │        │
│       │ - استخراج دقیق کد رهگیری بارنامه و کد ۵ رقمی OTP       │        │
│       │ - ارزیابی فیلترهای سرشماره و کلمات کلیدی (Whitelist)  │        │
│       └───────────────────────────┬───────────────────────────┘        │
│                                   ▼                                    │
│       ┌───────────────────────────────────────────────────────┐        │
│       │ لایه داده آفلاین و پایگاه‌داده محلی (Room Database)    │        │
│       │ - ثبت وضعیت اولیه پیام (PENDING / PROCESSED)          │        │
│       └─────────────┬───────────────────────────┬─────────────┘        │
│                     │                           │                      │
│             (شبکه متصل است)             (قطع موقت آنتن جاده)           │
│                     │                           │                      │
│                     ▼                           ▼                      │
│       ┌───────────────────────────┐ ┌───────────────────────────┐      │
│       │ OkHttp Client             │ │ WorkManager Offline Queue │      │
│       │ - امضای HMAC-SHA256       │ │ - شرط NetworkType.CONNECTED│     │
│       │ - رمزنگاری AES-256-GCM    │ │ - الگوریتم بازتلاش نمایی │      │
│       │ - سکرت هدر X-Forwarder    │ │ - همگام‌سازی پس از اتصال │      │
│       └─────────────┬─────────────┘ └─────────────┬─────────────┘      │
└─────────────────────┼─────────────────────────────┼────────────────────┘
                      │                             │
                      └──────────────┬──────────────┘
                                     │ HTTPS POST /api/v1/rpa/sms-forwarder
                                     │ Headers: X-Forwarder-Secret, Content-Type: app/json
                                     ▼
┌────────────────────────────────────────────────────────────────────────┐
│                 سرور پردازش ابری بارپرو (FastAPI Production API)        │
│                                                                        │
│   1. ارزیابی هدر امنیتی با مقایسه زمان‌ثابت (Constant-Time Verification)│
│   2. محافظت اندازه بدنه (< 64KB) و تایپ داده (JSON)                   │
│   3. اعتبارسنجی ارقام و ساختار شماره تلفن همراه ایران (09XXXXXXXXX)     │
│   4. استخراج و اعتبارسنجی کد ۵ رقمی (Strict 5-Digit Contextual Regex)  │
│   5. بررسی اثر انگشت جهت جلوگیری از پردازش تکراری (Idempotency)        │
└─────────────────────┬───────────────────────────┬──────────────────────┘
                      │                           │
        (ثبت مقتدرانه با TTL 180s)         (پخش رویداد سریع Pub/Sub)
                      │                           │
                      ▼                           ▼
┌────────────────────────────────────────────────────────────────────────┐
│                      صندوق رمزارز و رویداد ردیس (Redis 7 Vault)        │
│                                                                        │
│   - Key: rpa:otp:{phone} یا rpa:otp:{phone}:{doc_id}                  │
│   - Channel: rpa:otp:channel:{phone}                                   │
│   - Idempotency Key: rpa:otp:idempotency:{sha256_fingerprint}         │
└─────────────────────┬───────────────────────────┬──────────────────────┘
                      │                           │
                      │                           ▼
                      │             ┌─────────────────────────────┐
                      │             │ مشترک اختصاصی Pub/Sub       │
                      │             │ (Dedicated Async Queue)     │
                      │             └─────────────┬───────────────┘
                      │                           │ دریافت آنی (< 5ms)
                      ▼                           ▼
┌────────────────────────────────────────────────────────────────────────┐
│         موتور اتوماسیون بارنامه با پلی‌رایت (EnhancedWaybillManager)    │
│                                                                        │
│   - ورود به پرتال صدور بارنامه برخط شهرداری (UTCMS)                     │
│   - تکمیل مشخصات بار، ناوگان، مبدا، مقصد و کرایه                       │
│   - انتظار برای رویداد OTP (حداکثر ۹۰ ثانیه)                           │
│   - تزریق مستقیم به فیلد #otp و ارسال دکمه تایید نهایی                 │
│   - تخلیه اتمیک کد از ردیس (GETDEL) جهت تضمین تک‌بار مصرف بودن         │
│   - دریافت کد رهگیری بارنامه و تغییر وضعیت به COMPLETED               │
│   - راهبرد Fail-Closed در صورت انقضای زمان به NEEDS_REVIEW             │
└────────────────────────────────────────────────────────────────────────┘
```

---

## ۲. لایه‌بندی معماری کلاینت اندروید (Android Client Architecture)

اپلیکیشن اندروید با الگوگیری دقیق از **Clean Architecture** و **MVVM** به گونه‌ای طراحی شده که حتی در بدترین شرایط ارتباطی رانندگان در جاده‌ها (قطع آنتن، اتمام بسته اینترنت، راه‌اندازی مجدد گوشی و مدیریت سختگیرانه حافظه اندروید) هیچ پیامی مفقود نگردد:

### ۲.۱ ماژول دریافت دوگانه با احتمال خطای صفر (Dual-Path Zero-Loss Reception)
- **مسیر اولیه (`SmsReceiver`)**: شنونده برودکست‌های سیستمی `Telephony.SMS_RECEIVED` با بالاترین اولویت سیستمی (`android:priority="999"`).
- **مسیر پشتیبان (`SmsNotificationListener`)**: در دستگاه‌های مجهز به اندروید ۱۱، ۱۲، ۱۳ و ۱۴ که ممکن است مدیریت انرژی سازنده گوشی برودکست‌ها را به تاخیر بیندازد، نوتیفیکیشن دریافتی از اپلیکیشن پیام‌رسان پیش‌فرض را بلافاصله اسکن و پردازش می‌کند.
- **سرویس مداوم فورگراند (`SmsForwarderService`)**: ایجاد یک اعلان فعال دائمی در نوار وضعیت جهت جلوگیری از کشته شدن برنامه توسط فرآیند OOM (Out of Memory) اندروید.
- **راه‌اندازی خودکار پس از بوت (`BootReceiver`)**: گوش دادن به رویداد `BOOT_COMPLETED` تا سرویس به محض روشن شدن گوشی راننده بدون نیاز به باز کردن دستی اپ فعال شود.

### ۲.۲ پارسر هوشمند و نرمال‌سازی متون فارسی (`SmsParser`)
- **نرمال‌سازی ارقام**: ارقام یونیکد فارسی (`\u06F0` تا `\u06F9`) و عربی (`\u0660` تا `\u0669`) و همچنین نیم‌فاصله‌ها (`\u200C`) پیش از اعمال عبارات منظم به ارقام استاندارد لاتین تبدیل می‌شوند.
- **دسته‌بندی هوشمند (`SmsType`)**:
  - `UTCMS_CONFIRMATION`: تاییدیه صدور یا ترخیص بارنامه
  - `UTCMS_OTP`: کدهای رمز یکبار مصرف ۵ تا ۸ رقمی
  - `UTCMS_WARNING`: هشدارهای ابطال، مغایرت یا کسر سهمیه سوخت ناوگان
  - `OTHER`: پیامک‌های عمومی

### ۲.۳ معماری آفلاین و ارسال تضمینی (Offline-First Resilient Pipeline)
- هر پیام بلافاصله در دیتابیس محلی **Room** ذخیره می‌شود.
- در صورت در دسترس بودن اینترنت، بسته فوراً از طریق کلاینت OkHttp ارسال می‌شود.
- در صورت عدم برقراری اتصال یا خطاهای شبکه، پیام در وضعیت `PENDING` باقی مانده و توسط `WorkManager` با شرط اتصال به شبکه (`NetworkType.CONNECTED`) و استراتژی تاخیر نمایی (`BackoffPolicy.EXPONENTIAL`) مجدداً ارسال خواهد شد.

---

## ۳. معماری امنیتی و رمزنگاری (Security Architecture)

### ۳.۱ احراز هویت وب‌هوک با مقایسه زمان‌ثابت (Constant-Time Secret Verification)
برای ممانعت از حملات تحلیل زمانی (Timing Attacks)، ارزیابی هدر `X-Forwarder-Secret` با استفاده از `hmac.compare_digest` در پایتون و مقایسه امن بایتی انجام می‌گیرد. مقادیر پیش‌فرض یا آسیب‌پذیر (مانند `secret`, `123456`) در حالت پروداکشن به صورت Fail-Closed مسدود می‌شوند.

### ۳.۲ امضای دیجیتال HMAC-SHA256 (`X-Signature`)
برای اتصال به وب‌سرویس‌های اصلی بارپرو، بدنه JSON پیام به همراه کلید مخفی راننده توسط تابع `SignatureUtils.generateSignature` هش شده و در هدر قرار می‌گیرد تا هرگونه دستکاری در مسیر شبکه بلافاصله کشف گردد.

### ۳.۳ رمزنگاری متقارن سرتاسری AES-256-GCM (`AesEncryptionUtils`)
در صورت فعال بودن سوییچ رمزنگاری محتوا، بدنه پیامک پیش از خروج از دستگاه اندرویدی راننده با استفاده از الگوریتم احراز هویت‌شده AES-256 در مد GCM رمزنگاری می‌شود.

### ۳.۴ اصل عدم افشای کد OTP و حریم خصوصی
- کد خام OTP هرگز در پاسخ‌های JSON وب‌هوک به کلاینت یا ابزارهای مانیتورینگ بازگردانده نمی‌شود.
- کلیه شماره تلفن‌های ثبت‌شده در لاگ‌ها به صورت ماسک‌شده (`0933***2137`) ذخیره می‌گردند.
- فیلترهای سفید پیامک تضمین می‌کنند که پیام‌های شخصی و غیرمرتبط راننده از دستگاه خارج نشوند.

---

## ۴. معماری صندوق امن OTP در ردیس (Redis OTP Vault & Pub/Sub)

```
                       ┌────────────────────────────────┐
                       │   پیامک وب‌هوک از کلاینت راننده │
                       └───────────────┬────────────────┘
                                       │
                                       ▼
                       ┌────────────────────────────────┐
                       │  تولید کلید انگشت‌نگاری (SHA256) │
                       │ rpa:otp:idempotency:{fingerprint}│
                       └───────────────┬────────────────┘
                                       │
                      ┌────────────────┴────────────────┐
             (آیا تکراری است؟)                 (پیام جدید است)
                      │                                 │
                      ▼                                 ▼
         ┌─────────────────────────┐       ┌─────────────────────────┐
         │ بازگرداندن پاسخ Idempotent│       │  واریز به کلید مقتدرانه  │
         │ is_duplicate: true      │       │ rpa:otp:{correlation_key│
         │ HTTP 200 OK             │       │ TTL: 180 Seconds        │
         └─────────────────────────┘       └────────────┬────────────┘
                                                        │
                                                        ▼
                                           ┌─────────────────────────┐
                                           │ انتشار بلادرنگ رویداد   │
                                           │ روی کانال Pub/Sub ردیس  │
                                           │ rpa:otp:channel:{corr}  │
                                           └─────────────────────────┘
```

### ۴.۱ جلوگیری از تداخل فرآیندها (Race-Free Wait & Reconciliation)
یکی از چالش‌های سامانه‌های توزیع‌شده، تاخیر یا تقدم دریافت پیامک نسبت به تکمیل فرم در مرورگر است:
1. **پیامک زودتر از ربات می‌رسد**: کد در کلید ردیس (`rpa:otp:{phone}`) ذخیره شده است. هنگام فراخوانی `wait_for_otp`، ابتدا کش مقتدرانه خوانده می‌شود و اگر موجود بود، بلافاصله بازگردانده می‌شود بدون اینکه ربات معطل بماند.
2. **پیامک در حین کار ربات می‌رسد**: سابسکرایبر اختصاصی به کانال Pub/Sub گوش می‌دهد و به محض انتشار پیامک، آن را در کمتر از چند میلی‌ثانیه تحویل می‌گیرد.
3. **همپوشانی و رقابت (Race Window)**: حتی اگر پیامی دقیقاً بین زمان بررسی کش و اشتراک در کانال برسد، سیستم مجدداً کش را بازبینی می‌کند (Race Recheck) تا هیچ رویدادی از دست نرود.

### ۴.۲ مصرف تک‌باره (Single-Use Atomic Consumption)
به منظور رعایت الزامات امنیت سایبری سامانه‌های بانکی و حمل‌ونقل، به محض تایید موفقیت‌آمیز ورود کد در سامانه بارنامه، کلید OTP از طریق دستور اتمیک `GETDEL` از ردیس حذف می‌شود تا از استفاده مجدد جلوگیری به عمل آید.

---

## ۵. ماشین وضعیت اتوماسیون بارنامه (RPA State Machine)

فرآیند ثبت بارنامه توسط `EnhancedWaybillManager` هدایت شده و وضعیت‌ها با نگاشت قطعی مطابق جدول زیر تغییر می‌کنند:

| وضعیت (`WaybillStatus`) | رویداد خروجی (`WaybillOutcome`) | شرح و اقدام بعدی |
|---|---|---|
| `PENDING` | - | آغاز فرآیند و ایجاد کانتکست بارنامه |
| `SUBMITTING` | - | باز کردن مرورگر Playwright و درج اطلاعات بار و راننده |
| `OTP_PENDING` | `OTP_WAITING` | فرم منتظر دریافت کد ۵ رقمی پیامک از راننده است |
| `FINALIZING` | `OTP_SUBMITTED` / `OTP_ACCEPTED` | کد به فیلد `#otp` تزریق شده و دکمه تایید فشرده شده است |
| `COMPLETED` | `TRACKING_CODE_RECEIVED` | صدور موفق بارنامه؛ کد رهگیری دریافت و ثبت شد |
| `NEEDS_REVIEW` | `OTP_TIMEOUT` | سپری شدن ۹۰ ثانیه بدون دریافت پیامک راننده |
| `NEEDS_REVIEW` | `OTP_REJECTED` | سامانه UTCMS کد را نپذیرفت (نیاز به بازبینی اپراتور) |
| `NEEDS_REVIEW` | `PLAYWRIGHT_FAILURE` / `REDIS_FAILURE` | خطای زیرساختی مرورگر یا پایگاه داده |

این معماری تضمین می‌کند که فرآیندها در صورت بروز هرگونه اختلال به صورت **Fail-Closed** رفتار کرده و هرگز وضعیت نامشخص در سیستم باقی نماند.


