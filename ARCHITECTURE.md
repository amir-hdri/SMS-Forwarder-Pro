# BarPro SMS Forwarder - معماری فنی سیستم (Technical Architecture)

## ۱. نمای کلی سیستم (System Overview)

```
[سیستم مخابرات / SMS Gateway]
           │
           ▼
┌────────────────────────────────────────────────────────┐
│               دستگاه اندروید راننده / ناوگان            │
│                                                        │
│  ┌─────────────────────────┐  ┌─────────────────────┐  │
│  │   SmsReceiver (SMS)     │  │ SmsNotificationList.│  │
│  └───────────┬─────────────┘  └──────────┬──────────┘  │
│              │ (Dual-Path Reception)     │             │
│              ▼                           ▼             │
│  ┌──────────────────────────────────────────────────┐  │
│  │     SmsParser & Normalizer (UTCMS / BarPro)      │  │
│  │     - فارسی‌سازی/عربی‌سازی ارقام                    │  │
│  │     - استخراج کد رهگیری بارنامه و رمز OTP          │  │
│  │     - دسته‌بندی نوع پیامک (SmsType)                │  │
│  └──────────────────────────┬───────────────────────┘  │
│                             ▼                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  SmsForwardRepository & Local Room DB (Offline)  │  │
│  └───────────┬──────────────────────────┬───────────┘  │
│              │                          │              │
│    (اینترنت برقرار است)          (عدم پوشش جاده‌ای)    │
│              │                          │              │
│              ▼                          ▼              │
│  ┌───────────────────────┐  ┌───────────────────────┐  │
│  │ OkHttp / Retrofit     │  │ WorkManager           │  │
│  │ - HMAC-SHA256 Sign    │  │ - Exponential Backoff │  │
│  │ - AES-256 Encrypt     │  │ - Network Constraint  │  │
│  │ - Device & Driver Id  │  │ - Auto-Retry on Conn  │  │
│  └───────────┬───────────┘  └───────────┬───────────┘  │
│              │                          │              │
└──────────────┼──────────────────────────┼──────────────┘
               │                          │
               ▼                          ▼
┌────────────────────────────────────────────────────────┐
│            سرور مرکزی بارپرو (BarPro Server API)        │
│          Endpoint: /api/v1/sms/forward                 │
│                                                        │
│  - اعتبارسنجی امضای HMAC-SHA256                        │
│  - تطبیق کد رهگیری بارنامه با راننده ناوگان           │
│  - ثبت وضعیت و پاسخ وضعیت موفق ۲xx                     │
└────────────────────────────────────────────────────────┘
```

## ۲. مشخصات لایه‌ها و ماژول‌ها

### ۲.۱ ماژول دریافت (Reception Module)
- **مسیر اولیه (Primary)**: `SmsReceiver` با لیسن کردن به `android.provider.Telephony.SMS_RECEIVED`.
- **مسیر پشتیبان (Secondary)**: `SmsNotificationListener` برای دور زدن محدودیت‌های شدید بک‌گراند در نسخه‌های جدید اندروید (۱۱، ۱۲، ۱۳، ۱۴).
- **فوروارد فورگراند**: `ForegroundForwarderService` با نوتیفیکیشن دائم جهت جلوگیری از توقف پردازش توسط سیستم‌عامل.

### ۲.۲ ماژول پارس و هوشمندی (`SmsParser`)
- **نرمال‌سازی ارقام**: تبدیل خودکار کاراکترهای یونیکد ۰-۹ فارسی (`\u06F0` تا `\u06F9`) و عربی (`\u0660` تا `\u0669`) به ارقام استاندارد انگلیسی جهت تحلیل دقیق عبارات منظم.
- **الگوهای منظم اختصاصی**:
  - تطبیق کد رهگیری سامانه UTCMS بارنامه.
  - استخراج کدهای یکبار مصرف ۴ تا ۸ رقمی.
  - تشخیص هشدارهای باطل شدن، انقضا، یا مغایرت بارنامه.

### ۲.۳ ماژول امنیت (`crypto` & `utils`)
- **امضای پیامک**: `SignatureUtils.generateSignature(payload, secretKey)` بر مبنای `HmacSHA256` با خروجی هگزادسیمال کوچک.
- **تایید اصالت در سرور**: سرور با کلید اختصاصی ذخیره‌شده دستگاه و راننده، هش را مجددا محاسبه و اعتبارسنجی می‌کند.
- **ممیزی سلامت دستگاه**: `SecurityUtils.getSecurityReport(context)` تست دسترسی Root، فایل‌های su/busybox، حالت Developer/Debug و محیط شبیه‌ساز را گزارش می‌دهد.

### ۲.۴ ماژول صف آفلاین و تضمین ارسال (`service/SmsSyncWorker`)
- استفاده از `WorkManager` با شرط `NetworkType.CONNECTED`.
- تعریف `BackoffPolicy.EXPONENTIAL` با حداقل تاخیر ۳۰ ثانیه‌ای و افزایش نمایی تا سقف ۵ بار تلاش مجدد.
- ذخیره کد وضعیت HTTP، متن خطای سرور و ثبت دقیق وضعیت موفق/ناموفق در لاگ دیتابیس Room.
